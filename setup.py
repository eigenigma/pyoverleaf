"""Setuptools entry point.

Configuration lives in ``pyproject.toml``; this file only exists so legacy
``pip``/``setuptools`` invocations that look for ``setup.py`` still work.
"""

from setuptools import setup

setup()
